import React from 'react';
import { NavLink } from 'react-router';

const Logo = () => {
    return <NavLink to="/" className="btn btn-ghost text-xl bg-amber-50">GOTRACK</NavLink>
};

export default Logo;